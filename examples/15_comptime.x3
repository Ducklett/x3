import "./lib/standard" use standard

proc main() {
	const v = 0 
	@if v == 0 {
		var number = @10 / 2 * 3 + 0
	} else if v == 1 {
		var number = 3*3
	} else {
		var number = 4
	}

	@assert(number == 4)

	printf("number: %\n", number)
}
