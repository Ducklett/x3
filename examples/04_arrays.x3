import "./lib/standard" use standard

proc print list[list:[]string]  {
    each item : list {
        write line(item)
    }
}

proc main {
    const names = ["foo", "bar", "baz", "quux"]

    print list(names)

    exit(names.length)
}
